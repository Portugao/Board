{# purpose of this template: inclusion template for display of related users #}
{% set hasAdminPermission = permissionHelper.hasComponentPermission('user', constant('ACCESS_EDIT')) %}
{% if noLink is not defined %}
    {% set noLink = false %}
{% endif %}
<h4>
{% spaceless %}
{% if not noLink %}
    <a href="{{ path('muboardmodule_user_' ~ routeArea ~ 'display', {'id': item.getId()}) }}" title="{{ item|muboardmodule_formattedTitle|e('html_attr') }}">
{% endif %}
    {{ item|muboardmodule_formattedTitle }}
{% if not noLink %}
    </a>
    <a id="userItem{{ item.getKey() }}Display" href="{{ path('muboardmodule_user_' ~ routeArea ~ 'display', {'id': item.getId(), raw: 1}) }}" title="{{ __('Open quick view window') }}" class="muboard-inline-window hidden" data-modal-title="{{ item|muboardmodule_formattedTitle|e('html_attr') }}"><i class="fa fa-id-card-o"></i></a>
{% endif %}
{% endspaceless %}
</h4>
