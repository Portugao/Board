{# purpose of this template: users view csv view #}
{% spaceless %}"{{ __('Userid') }}";"{{ __('Number postings') }}";"{{ __('Last visit') }}";"{{ __('Rank') }}"
{% endspaceless %}
{% for user in items %}

{% spaceless %}
    "{{ user.userid }}";"{{ user.numberPostings }}";"{{ user.lastVisit|localizeddate('medium', 'short') }}";"{% if user.rank|default %}{{ user.rank|muboardmodule_formattedTitle }}{% endif %}"
{% endspaceless %}
{% endfor %}
