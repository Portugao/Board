{# Purpose of this template: Display abos within an external context #}
{% for abo in items %}
    <h3>{{ abo|muboardmodule_formattedTitle }}</h3>
    <p><a href="{{ path('muboardmodule_abo_display', {'id': abo.getId()}) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
